---
phase: 11-helius-rpc-provider-rotation
plan: 04
type: execute
wave: 3
depends_on:
  - "11-02"
  - "11-03"
files_modified:
  - src/monitor/loop.ts
  - src/importers/history.ts
  - src/discovery/early-buyers.ts
  - src/discovery/graph-traverse.ts
  - src/detection/dev-wallet.ts
  - src/fetchers/providers/index.ts
  - .env.example
autonomous: true
requirements:
  - MNTR-03

must_haves:
  truths:
    - "MonitorLoop uses createProviderRouter() instead of createHeliusFetcher() — callers do not change their usage pattern"
    - "importWalletHistory uses createProviderRouter() instead of createHeliusFetcher()"
    - "discovery/early-buyers.ts uses createProviderRouter() for fetchEarlySwapsForMint"
    - "discovery/graph-traverse.ts uses createProviderRouter() for fetchOnePage and fetchEarlySwapsForMint"
    - "dev-wallet.ts getDefaultFetcher uses createProviderRouter() for fetchOnePage"
    - "bundler.ts is NOT migrated — BundlerFetcher requires getTransaction() which is not on RpcProvider"
    - "bundler.ts and wash-trader.ts getTransaction() calls remain on createHeliusFetcher() — not on provider router"
    - "All 184 original tests plus new provider tests pass after migration"
    - ".env.example documents SHYFT_API_KEY as optional"
  artifacts:
    - path: "src/monitor/loop.ts"
      provides: "MonitorLoop using ProviderRouter"
      contains: "createProviderRouter"
    - path: "src/importers/history.ts"
      provides: "importWalletHistory using ProviderRouter"
      contains: "createProviderRouter"
    - path: "src/discovery/early-buyers.ts"
      provides: "fetchEarlyBuyers using ProviderRouter (discovery orchestrator)"
      contains: "createProviderRouter"
    - path: "src/discovery/graph-traverse.ts"
      provides: "graph traversal using ProviderRouter (discovery orchestrator)"
      contains: "createProviderRouter"
    - path: "src/detection/dev-wallet.ts"
      provides: "getDefaultFetcher using createProviderRouter"
      contains: "createProviderRouter"
    - path: ".env.example"
      provides: "SHYFT_API_KEY documented"
      contains: "SHYFT_API_KEY"
  key_links:
    - from: "src/monitor/loop.ts"
      to: "src/fetchers/providers/index.ts"
      via: "import createProviderRouter"
      pattern: "createProviderRouter"
    - from: "src/importers/history.ts"
      to: "src/fetchers/providers/index.ts"
      via: "import createProviderRouter"
      pattern: "createProviderRouter"
    - from: "src/discovery/early-buyers.ts"
      to: "src/fetchers/providers/index.ts"
      via: "import createProviderRouter (replaces createHeliusFetcher)"
      pattern: "createProviderRouter"
    - from: "src/discovery/graph-traverse.ts"
      to: "src/fetchers/providers/index.ts"
      via: "import createProviderRouter (replaces createHeliusFetcher)"
      pattern: "createProviderRouter"
    - from: "src/detection/dev-wallet.ts"
      to: "src/fetchers/providers/index.ts"
      via: "lazy import createProviderRouter"
      pattern: "createProviderRouter"
---

<objective>
Migrate the callsites that use createHeliusFetcher() to createProviderRouter(). This wires the provider abstraction into the live system — after this plan, provider rotation is active end-to-end.

Purpose: Plans 01-03 built the abstraction. This plan activates it. No new logic — pure callsite rewiring.
Output: 5 modified callsites (loop, history, early-buyers, graph-traverse, dev-wallet), updated .env.example, and a clean full regression run proving 184+ tests green.

IMPORTANT scope boundary: bundler.ts is NOT migrated. The BundlerFetcher interface requires only getTransaction() — which is not on RpcProvider. bundler.ts must remain on createHeliusFetcher() for its getDefaultFetcher. Do not touch bundler.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-helius-rpc-provider-rotation/11-RESEARCH.md
@.planning/phases/11-helius-rpc-provider-rotation/11-01-SUMMARY.md
@.planning/phases/11-helius-rpc-provider-rotation/11-02-SUMMARY.md
@.planning/phases/11-helius-rpc-provider-rotation/11-03-SUMMARY.md

<interfaces>
<!-- Critical: know what NOT to change -->

Files that must NOT be modified:
- src/fetchers/helius.ts — 184 tests test against it directly
- src/detection/wash-trader.ts — uses getTransaction() which is NOT on RpcProvider
- src/detection/bundler.ts — BundlerFetcher interface requires only getTransaction(). ProviderRouter does NOT implement getTransaction. Do NOT change bundler.ts at all.

Why bundler.ts stays on createHeliusFetcher():
BundlerFetcher is defined as:
```typescript
export interface BundlerFetcher {
  getTransaction: (signature: string) => Promise<{ ... }>;
}
```
getTransaction is NOT on the RpcProvider interface (locked decision). ProviderRouter cannot satisfy BundlerFetcher. bundler.ts getDefaultFetcher MUST remain on createHeliusFetcher().

Callsite map (5 locations to migrate):
1. src/monitor/loop.ts — import createHeliusFetcher → createProviderRouter; createHeliusFetcher() call → createProviderRouter()

2. src/importers/history.ts — import createHeliusFetcher → createProviderRouter; createHeliusFetcher() call → createProviderRouter()

3. src/discovery/early-buyers.ts — top-level import createHeliusFetcher → createProviderRouter;
   line ~45: `const f = fetcher ?? createHeliusFetcher();` → `const f = fetcher ?? createProviderRouter();`
   (fetcher parameter type stays as the local interface — ProviderRouter satisfies it since it has fetchEarlySwapsForMint)

4. src/discovery/graph-traverse.ts — top-level import createHeliusFetcher → createProviderRouter;
   line ~45: `const f = fetcher ?? createHeliusFetcher();` → `const f = fetcher ?? createProviderRouter();`
   (fetcher parameter type stays as the local interface — ProviderRouter satisfies it since it has fetchOnePage and fetchEarlySwapsForMint)

5. src/detection/dev-wallet.ts (getDefaultFetcher only):
   OLD:
   ```typescript
   async function getDefaultFetcher(): Promise<DevWalletFetcher> {
     const { createHeliusFetcher } = await import('../fetchers/helius.js');
     const f = createHeliusFetcher();
   ```
   NEW:
   ```typescript
   async function getDefaultFetcher(): Promise<DevWalletFetcher> {
     const { createProviderRouter } = await import('../fetchers/providers/index.js');
     const f = createProviderRouter();
   ```
   (The adapter object body after this line stays identical — fetchOnePage exists on ProviderRouter)

From src/fetchers/providers/index.ts (Plan 02 output):
```typescript
export function createProviderRouter(): ProviderRouter  // replacement for createHeliusFetcher()
export type { RpcProvider, ProviderTransaction } from './types.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate loop.ts and history.ts callsites</name>
  <files>src/monitor/loop.ts, src/importers/history.ts</files>
  <action>
    In src/monitor/loop.ts:
    - Change import: `import { createHeliusFetcher } from '../fetchers/helius.js';`
      → `import { createProviderRouter } from '../fetchers/providers/index.js';`
    - Change the createHeliusFetcher() call: `createHeliusFetcher()` → `createProviderRouter()`
    - No other changes — the rest of loop.ts uses fetcher.fetchSwapHistory() which ProviderRouter implements

    In src/importers/history.ts:
    - Change import: `import { createHeliusFetcher } from '../fetchers/helius.js';`
      → `import { createProviderRouter } from '../fetchers/providers/index.js';`
    - Change the createHeliusFetcher() call: `createHeliusFetcher()` → `createProviderRouter()`

    BEFORE making changes: read both files in full to confirm exact line numbers and that no other changes are needed. The import change must be exact — do not remove other imports on the same line.

    Run TypeScript check immediately after each file change to catch issues early.
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/echo-wallet-tracking && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    loop.ts and history.ts import createProviderRouter (not createHeliusFetcher);
    TypeScript compiles clean for these two files;
    grep -n "createHeliusFetcher" src/monitor/loop.ts src/importers/history.ts returns no results
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate discovery callsites (early-buyers.ts, graph-traverse.ts) + dev-wallet.ts + .env.example + full regression</name>
  <files>src/discovery/early-buyers.ts, src/discovery/graph-traverse.ts, src/detection/dev-wallet.ts, .env.example</files>
  <action>
    IMPORTANT: Read each file in full BEFORE editing to understand exact context. Only change the createHeliusFetcher import and call — do not touch any other logic.

    In src/discovery/early-buyers.ts:
    - Change top-level import: `import { createHeliusFetcher } from '../fetchers/helius.js';`
      → `import { createProviderRouter } from '../fetchers/providers/index.js';`
    - Change the default fetcher assignment (~line 45):
      `const f = fetcher ?? createHeliusFetcher();`
      → `const f = fetcher ?? createProviderRouter();`
    - The local fetcher interface in this file already declares fetchEarlySwapsForMint — ProviderRouter satisfies it. No interface changes needed.

    In src/discovery/graph-traverse.ts:
    - Change top-level import: `import { createHeliusFetcher } from '../fetchers/helius.js';`
      → `import { createProviderRouter } from '../fetchers/providers/index.js';`
    - Change the default fetcher assignment (~line 45):
      `const f = fetcher ?? createHeliusFetcher();`
      → `const f = fetcher ?? createProviderRouter();`
    - The local fetcher interface in this file declares fetchOnePage and fetchEarlySwapsForMint — ProviderRouter satisfies both. No interface changes needed.

    In src/detection/dev-wallet.ts (getDefaultFetcher only):
    - Change the lazy import and factory call:
      OLD:
      ```typescript
      async function getDefaultFetcher(): Promise<DevWalletFetcher> {
        const { createHeliusFetcher } = await import('../fetchers/helius.js');
        const f = createHeliusFetcher();
      ```
      NEW:
      ```typescript
      async function getDefaultFetcher(): Promise<DevWalletFetcher> {
        const { createProviderRouter } = await import('../fetchers/providers/index.js');
        const f = createProviderRouter();
      ```
    - The adapter object body after this line stays identical — fetchOnePage exists on ProviderRouter
    - Do NOT touch any getTransaction() calls elsewhere in dev-wallet.ts

    DO NOT touch src/detection/bundler.ts — BundlerFetcher requires getTransaction() which is not on RpcProvider. bundler.ts stays on createHeliusFetcher() permanently.

    Update .env.example: add SHYFT_API_KEY entry after HELIUS_API_KEY:
    ```
    # Shyft API Key (Optional — fallback RPC provider for Helius outages)
    # Get a free key from https://shyft.to
    # If not set, system runs Helius-only and logs a warning at startup
    SHYFT_API_KEY=
    ```

    Full regression run after all changes:
    - `pnpm test` must pass with 184+ tests green
    - `npx tsc --noEmit` must pass with no errors
    - grep confirms no remaining createHeliusFetcher in loop.ts, history.ts, early-buyers.ts, graph-traverse.ts, dev-wallet.ts getDefaultFetcher
    - grep confirms createHeliusFetcher STILL EXISTS in bundler.ts and wash-trader.ts (these must NOT be changed)
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/echo-wallet-tracking && pnpm test 2>&1 | tail -30</automated>
  </verify>
  <done>
    Full test suite passes (184+ tests green, 0 failures);
    TypeScript compiles clean;
    5 callsites use createProviderRouter (loop, history, early-buyers, graph-traverse, dev-wallet);
    bundler.ts and wash-trader.ts getTransaction() paths unchanged and still use createHeliusFetcher;
    .env.example documents SHYFT_API_KEY
  </done>
</task>

</tasks>

<verification>
Final phase verification:
1. `pnpm test` passes — count must be >= 184 (new provider tests add to total)
2. `npx tsc --noEmit` passes with no type errors
3. `grep -rn "createHeliusFetcher" src/monitor/loop.ts src/importers/history.ts src/discovery/early-buyers.ts src/discovery/graph-traverse.ts` → 0 results
4. `grep -n "createProviderRouter" src/monitor/loop.ts src/importers/history.ts src/discovery/early-buyers.ts src/discovery/graph-traverse.ts src/detection/dev-wallet.ts` → 5 results (one per file)
5. `grep -n "getTransaction\|createHeliusFetcher" src/detection/bundler.ts src/detection/wash-trader.ts` → results exist (these callsites intentionally unchanged)
6. `grep "createHeliusFetcher" src/detection/bundler.ts` → result exists (bundler permanently stays on createHeliusFetcher)
7. `grep "SHYFT_API_KEY" .env.example` → result exists
</verification>

<success_criteria>
- 5 callsites migrated to createProviderRouter(): loop.ts, history.ts, early-buyers.ts, graph-traverse.ts, dev-wallet.ts
- bundler.ts is NOT migrated — BundlerFetcher.getTransaction() is not on RpcProvider (locked decision honored)
- getTransaction() paths in bundler.ts and wash-trader.ts remain on createHeliusFetcher()
- Full test suite passes (184+ tests) with zero regressions
- Provider rotation is live end-to-end: MonitorLoop, importWalletHistory, discovery orchestrator (both files), dev-wallet
- System warns on missing SHYFT_API_KEY and runs Helius-only until key is added
</success_criteria>

<output>
After completion, create `.planning/phases/11-helius-rpc-provider-rotation/11-04-SUMMARY.md`
</output>
