---
phase: 13-railway-deployment
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/railway-deployment.md
autonomous: true
gap_closure: true
requirements:
  - DEPLOY-03

must_haves:
  truths:
    - "REQUIREMENTS.md DEPLOY-03 accurately describes the warning-only behavior that actually ships"
    - "ROADMAP.md Phase 13 success criterion 3 matches the implementation and the documented rationale"
    - "docs/railway-deployment.md explains why replica detection warns rather than hard-fails"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated DEPLOY-03 requirement text — warning-only, not hard-fail"
      contains: "logs a WAL integrity warning"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 13 success criterion 3 — warning-only language"
      contains: "logs a WAL integrity warning"
    - path: "docs/railway-deployment.md"
      provides: "Operator-facing explanation of replica detection behaviour"
      contains: "RAILWAY_REPLICA_ID"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "src/cli.ts"
      via: "DEPLOY-03 text matches console.warn implementation"
      pattern: "logs a WAL integrity warning"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Success criterion 3 mirrors DEPLOY-03 updated text"
      pattern: "logs a WAL integrity warning"
---

<objective>
Resolve the DEPLOY-03 requirement contract mismatch identified in VERIFICATION.md.

The implementation emits `console.warn` when `RAILWAY_REPLICA_ID` is set — it does NOT call `process.exit(1)`. This is the correct behaviour: `RAILWAY_REPLICA_ID` is present on ALL Railway deployments (including single-replica), so hard-failing would prevent the service from ever starting on Railway. This rationale is a LOCKED decision from CONTEXT.md.

The gap is documentation-only: REQUIREMENTS.md and ROADMAP.md both say "refuses to start", which does not match what ships. This plan updates both documents (and the runbook) so the requirement contract accurately reflects the warning-only implementation.

Purpose: Close the DEPLOY-03 partial gap so phase 13 verification passes with 4/4 truths satisfied.
Output: Updated .planning/REQUIREMENTS.md, .planning/ROADMAP.md, and docs/railway-deployment.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-railway-deployment/13-VERIFICATION.md
@docs/railway-deployment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DEPLOY-03 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Read .planning/REQUIREMENTS.md, then make two targeted edits:

    1. Line 13 — change the DEPLOY-03 requirement text from:
       "System refuses to start with WAL mode if Railway replica count > 1"
       to:
       "System logs a WAL integrity warning if a Railway replica environment is detected (RAILWAY_REPLICA_ID set); does not hard-fail because RAILWAY_REPLICA_ID is present on all Railway deployments including single-replica"

    2. Add a comment line directly below DEPLOY-03 (still within the Deployment section) to record the rationale in a concise form for future readers:
       (no separate comment needed — the inline description is sufficient)

    The traceability table entry for DEPLOY-03 (line 79) already shows Status: Complete — leave it as-is.

    Also update the footer timestamp at the bottom of the file:
    "Last updated: 2026-04-01 — DEPLOY-03 requirement text updated to reflect warning-only replica detection (hard-fail not feasible: RAILWAY_REPLICA_ID present on all Railway deployments)"
  </action>
  <verify>
    grep -n "logs a WAL integrity warning" /Users/irfanmurad/Developer/vessl/echo-wallet-tracking/.planning/REQUIREMENTS.md
  </verify>
  <done>REQUIREMENTS.md DEPLOY-03 line contains "logs a WAL integrity warning" and no longer says "refuses to start"</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 13 success criterion 3 in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Read .planning/ROADMAP.md, then make two targeted edits:

    1. Under "### Phase 13: Railway Deployment", success criterion 3 currently reads:
       "3. If Railway replica count exceeds 1, the service refuses to start and logs a WAL integrity warning"

       Change it to:
       "3. If a Railway replica environment is detected (RAILWAY_REPLICA_ID set), the service logs a WAL integrity warning; the service does not hard-fail because RAILWAY_REPLICA_ID is present on all Railway deployments including single-replica"

    2. The progress table row for Phase 13 (line 102) currently reads malformed:
       "| 13. Railway Deployment             | 3/3 | Complete   | 2026-04-01 | -          |"

       Fix it to match the column order (Milestone | Plans Complete | Status | Completed) used by rows above it:
       "| 13. Railway Deployment             | v1.1      | 4/4            | Complete    | 2026-04-01 |"

       (Plans count is now 4 — three execution plans plus this gap closure plan.)

    Do not change any other text in ROADMAP.md.
  </action>
  <verify>
    grep -n "logs a WAL integrity warning" /Users/irfanmurad/Developer/vessl/echo-wallet-tracking/.planning/ROADMAP.md
  </verify>
  <done>ROADMAP.md Phase 13 success criterion 3 contains "logs a WAL integrity warning" and no longer says "refuses to start"; progress table row for Phase 13 is well-formed</done>
</task>

<task type="auto">
  <name>Task 3: Add replica-detection section to railway-deployment.md</name>
  <files>docs/railway-deployment.md</files>
  <action>
    Read docs/railway-deployment.md. Append a new troubleshooting entry at the end of the Troubleshooting section (after the "Service exits immediately on startup" block, before the final blank line):

    ```
    ### WAL integrity warning on startup

    On every Railway deployment (including single-replica), Railway injects `RAILWAY_REPLICA_ID` into the container environment. The `serve` command detects this variable and emits a console warning:

    ```
    [warn] RAILWAY_REPLICA_ID detected — WAL mode is not safe with multiple replicas sharing one SQLite file. Railway blocks volumes+replicas at the infrastructure level, but if you have manually enabled replicas, disable them before deploying.
    ```

    This is advisory only — the service continues to start. Railway itself prevents you from attaching a volume to a multi-replica service at the platform level, so the warning is a belt-and-suspenders notice to the operator. If you see this warning and have NOT intentionally configured multiple replicas, you can safely ignore it.
    ```

    Write the updated file. Do not alter any existing content above the new section.
  </action>
  <verify>
    grep -n "RAILWAY_REPLICA_ID" /Users/irfanmurad/Developer/vessl/echo-wallet-tracking/docs/railway-deployment.md
  </verify>
  <done>docs/railway-deployment.md contains a "WAL integrity warning on startup" troubleshooting section that references RAILWAY_REPLICA_ID and explains the advisory-only behaviour</done>
</task>

</tasks>

<verification>
After all three tasks complete, confirm:

1. `grep "refuses to start" .planning/REQUIREMENTS.md` returns no matches
2. `grep "refuses to start" .planning/ROADMAP.md` returns no matches
3. `grep "logs a WAL integrity warning" .planning/REQUIREMENTS.md` returns the DEPLOY-03 line
4. `grep "logs a WAL integrity warning" .planning/ROADMAP.md` returns the success criterion 3 line
5. `grep "RAILWAY_REPLICA_ID" docs/railway-deployment.md` returns the new troubleshooting section

No code changes. No test runs required. This is a documentation alignment plan.
</verification>

<success_criteria>
- REQUIREMENTS.md DEPLOY-03 no longer says "refuses to start" — says "logs a WAL integrity warning" with rationale
- ROADMAP.md Phase 13 success criterion 3 mirrors the updated requirement text
- docs/railway-deployment.md has a troubleshooting section explaining the warning-only replica detection and why RAILWAY_REPLICA_ID is always present
- Phase 13 VERIFICATION.md gap (DEPLOY-03 partial) is now closed: the requirement contract matches the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/13-railway-deployment/13-04-SUMMARY.md` documenting:
- What was changed (REQUIREMENTS.md, ROADMAP.md, docs/railway-deployment.md)
- The rationale preserved: RAILWAY_REPLICA_ID is present on ALL Railway deployments; hard-fail is not feasible
- That DEPLOY-03 is now fully satisfied with warning-only behaviour
</output>
