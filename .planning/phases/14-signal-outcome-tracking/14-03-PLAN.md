---
phase: 14-signal-outcome-tracking
plan: "03"
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - src/signals/accuracy.ts
  - src/signals/__tests__/accuracy.test.ts
  - src/api/views/partials/accuracy_stats.ejs
autonomous: true
requirements: [OUTCOME-03, OUTCOME-06]

must_haves:
  truths:
    - "getAccuracyStats() returns hit rates and average returns for all four time windows: 30m, 1h, 4h, 24h"
    - "Rug outcomes are excluded from accuracy denominators — WHERE is_rug = false OR is_rug IS NULL"
    - "TierAccuracy interface includes hits_30m, hit_rate_30m, avg_return_30m fields"
    - "Dashboard accuracy section renders a four-column table (30m/1h/4h/24h) with hit rates per tier"
    - "Accuracy stats route /api/accuracy/partial passes updated stats to accuracy_stats.ejs"
  artifacts:
    - path: "src/signals/accuracy.ts"
      provides: "Extended TierAccuracy interface and getAccuracyStats() with 4-window support and rug exclusion"
      contains: "hits_30m, hit_rate_30m, avg_return_30m, is_rug"
    - path: "src/signals/__tests__/accuracy.test.ts"
      provides: "Tests for rug exclusion from denominators and 30m accuracy statistics"
    - path: "src/api/views/partials/accuracy_stats.ejs"
      provides: "Dashboard accuracy partial with 4-window table layout"
      contains: "30m"
  key_links:
    - from: "src/api/views/partials/accuracy_stats.ejs"
      to: "src/api/routes/accuracy.ts"
      via: "/api/accuracy/partial HTMX route passing stats to EJS partial"
      pattern: "reply.view.*accuracy_stats"
    - from: "src/signals/accuracy.ts"
      to: "src/db/schema.ts"
      via: "is_rug column in WHERE filter, outcome_30m columns in SELECT"
      pattern: "signal_events.is_rug"
---

<objective>
Extend accuracy.ts to produce 4-window accuracy statistics (30m/1h/4h/24h) with rug tokens excluded from all denominators, and update the dashboard EJS partial to render the new 4-column table.

Purpose: Closes OUTCOME-03 (rug exclusion from accuracy) and OUTCOME-06 (multi-timeframe accuracy display). Accuracy is the primary forward-testing output surface — it must reflect real performance without survivorship bias from rugged tokens.
Output: Extended TierAccuracy interface, updated getAccuracyStats() with rug exclusion, updated accuracy_stats.ejs with 4-column layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-signal-outcome-tracking/14-CONTEXT.md
@.planning/phases/14-signal-outcome-tracking/14-RESEARCH.md
@.planning/phases/14-signal-outcome-tracking/14-01-SUMMARY.md

<interfaces>
<!-- Current TierAccuracy interface (from src/signals/accuracy.ts lines 18-26) -->
```typescript
export const MIN_SAMPLE = 20;

export interface TierAccuracy {
  tier: string;
  total_resolved: number;
  hits_24h: number;
  hit_rate_24h: number | null; // null if total_resolved < MIN_SAMPLE
  avg_return_1h: number | null;
  avg_return_4h: number | null;
  avg_return_24h: number | null;
}

export function getAccuracyStats(db: typeof defaultDb = defaultDb): TierAccuracy[]
```

<!-- Current WHERE filter in getAccuracyStats() -->
.where(and(
  eq(signal_events.is_fully_resolved, true),
  isNotNull(signal_events.entry_price),
))
// ADD to this: eq(signal_events.is_rug, false) — per user decision, rug outcomes excluded from denominator

<!-- accuracy_stats.ejs is at: src/api/views/partials/accuracy_stats.ejs -->
<!-- accuracy route at: src/api/routes/accuracy.ts -->
<!-- /api/accuracy/partial passes: { stats, recentEvents, MIN_SAMPLE } to the partial -->

<!-- Dashboard pattern from Phase 12: HTMX SSE refresh on sse:cycle event -->
<!-- EJS helpers already in accuracy_stats.ejs: fmtPct(val), fmtStatus(status) -->

<!-- User decision on hit definition: configurable — read from env -->
<!-- Per CONTEXT.md: "Hit" definition is user-configurable -->
<!-- Implementation choice (Claude's Discretion): read HIT_THRESHOLD_PCT from env (default: same as tier thresholds) -->
<!-- Simplest approach: use existing HIT_THRESHOLDS from outcome-resolver (strong=50%, moderate=25%, weak=any) — dashboard shows tier-specific hit rates which already embody the configured thresholds -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TierAccuracy and getAccuracyStats() for 4-window rug-excluded stats</name>
  <files>src/signals/accuracy.ts, src/signals/__tests__/accuracy.test.ts</files>
  <behavior>
    - Test: signal_events with is_rug=true are excluded from total_resolved count (rug exclusion from denominator)
    - Test: signal_events with is_rug=false are included in total_resolved
    - Test: getAccuracyStats() returns hits_30m count matching signals where outcome_30m_status='hit' (excluding rugs)
    - Test: hit_rate_30m is null when total_resolved < MIN_SAMPLE (20), consistent with hit_rate_24h behavior
    - Test: hit_rate_30m is a number between 0 and 1 when total_resolved >= MIN_SAMPLE
    - Test: avg_return_30m reflects average of outcome_30m_pct for non-rug resolved signals
    - Test: returns empty array when no is_fully_resolved=true signals exist
    - Test: returns null hit_rate_30m for tier with exactly MIN_SAMPLE - 1 resolved non-rug signals
  </behavior>
  <action>
**Before writing tests**, read `src/signals/__tests__/accuracy.test.ts` to understand the test helper pattern (in-memory SQLite with migrate(), insertTestSignalEvent helper).

Extend `src/signals/accuracy.ts`:

**1. Update TierAccuracy interface** — add 30m fields and update `total_resolved` doc:
```typescript
export interface TierAccuracy {
  tier: string;
  total_resolved: number;       // non-rug only (is_rug=false or null)
  hits_30m: number;
  hits_24h: number;
  hit_rate_30m: number | null;  // null if total_resolved < MIN_SAMPLE
  hit_rate_24h: number | null;
  avg_return_30m: number | null;
  avg_return_1h: number | null;
  avg_return_4h: number | null;
  avg_return_24h: number | null;
}
```
Note: hits_1h and hits_4h are intentionally omitted (only 30m and 24h have hit rates — 1h/4h show avg returns only, consistent with existing pattern). If the existing tests already assert on `hits_24h` shape, keep that consistent.

**2. Update getAccuracyStats() query** — two changes:
- Add `hits_30m: sql<number>` and `avg_return_30m: sql<number>` to the SELECT
- Add rug exclusion to WHERE: wrap existing filter with `or(eq(signal_events.is_rug, false), isNull(signal_events.is_rug))`

Full WHERE becomes:
```typescript
.where(and(
  eq(signal_events.is_fully_resolved, true),
  isNotNull(signal_events.entry_price),
  or(eq(signal_events.is_rug, false), isNull(signal_events.is_rug)),
))
```

**3. Update the return mapping** — add `hits_30m`, `hit_rate_30m`, `avg_return_30m` to the returned objects. Apply the same MIN_SAMPLE null guard to `hit_rate_30m` that already applies to `hit_rate_24h`.

Import `or` from `drizzle-orm` if not already imported.
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/echo-wallet-tracking && pnpm test -- --testPathPattern="accuracy" 2>&1</automated>
  </verify>
  <done>All accuracy tests pass. getAccuracyStats() returns TierAccuracy objects with hits_30m, hit_rate_30m, avg_return_30m. Rug events excluded from total_resolved. TypeScript compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update accuracy_stats.ejs to 4-window table layout</name>
  <files>src/api/views/partials/accuracy_stats.ejs</files>
  <action>
Read `src/api/views/partials/accuracy_stats.ejs` first to understand the current layout before modifying it.

Update the EJS partial to render a 4-column table with per-tier rows. The table structure:

**Table header:**
```
Tier | Signals | 30m Hit% | 1h Return | 4h Return | 24h Hit%
```

**Per-tier row logic:**
- `Tier`: stat.tier (capitalize first letter)
- `Signals`: stat.total_resolved (non-rug count)
- `30m Hit%`: `stat.hit_rate_30m !== null ? (stat.hit_rate_30m * 100).toFixed(1) + '%' : 'Insufficient data (' + stat.total_resolved + '/' + MIN_SAMPLE + ')'`
- `1h Return`: `stat.avg_return_1h !== null ? (stat.avg_return_1h * 100).toFixed(1) + '%' : '–'`
- `4h Return`: `stat.avg_return_4h !== null ? (stat.avg_return_4h * 100).toFixed(1) + '%' : '–'`
- `24h Hit%`: `stat.hit_rate_24h !== null ? (stat.hit_rate_24h * 100).toFixed(1) + '%' : 'Insufficient data (' + stat.total_resolved + '/' + MIN_SAMPLE + ')'`

Add a note below the table: `<p class="text-xs text-gray-500 mt-1">Rug outcomes excluded from all stats.</p>`

Keep the existing EJS helper functions (fmtPct, fmtStatus) if they are used elsewhere in the partial. If they are no longer needed after the update, leave them — don't break things trying to remove them.

Keep the HTMX SSE auto-refresh attributes that were established in Phase 12 (`hx-ext=sse`, `sse-connect`, `hx-trigger=sse:cycle`) — do not remove these.

**Sparse data handling (Claude's Discretion per CONTEXT.md):** Show "Insufficient data (N/20)" for null hit rates — this is the established pattern, use it consistently for both 30m and 24h columns.
  </action>
  <verify>
    <automated>cd /Users/irfanmurad/Developer/vessl/echo-wallet-tracking && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>accuracy_stats.ejs renders 4-column table with 30m Hit%, 1h Return, 4h Return, 24h Hit%. Rug exclusion note present. TypeScript compiles (EJS is not type-checked but route still compiles). pnpm test passes.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/irfanmurad/Developer/vessl/echo-wallet-tracking && pnpm test 2>&1 | tail -15
```
All tests pass. Accuracy stats returns 4-window data with rug exclusion.
</verification>

<success_criteria>
- TierAccuracy has hits_30m, hit_rate_30m, avg_return_30m fields
- getAccuracyStats() excludes is_rug=true rows from all aggregates
- accuracy_stats.ejs shows 4 time windows with "Insufficient data" for null hit rates
- Rug exclusion note visible in dashboard partial
- All accuracy tests pass including new rug exclusion assertions
</success_criteria>

<output>
After completion, create `.planning/phases/14-signal-outcome-tracking/14-03-SUMMARY.md`
</output>
