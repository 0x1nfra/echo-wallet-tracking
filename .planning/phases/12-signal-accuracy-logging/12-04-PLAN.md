---
phase: 12-signal-accuracy-logging
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - src/api/routes/accuracy.ts
  - src/api/views/partials/accuracy_stats.ejs
  - src/api/views/dashboard.ejs
  - src/api/server.ts
  - src/api/bot/commands.ts
autonomous: true
requirements: [QUAL-02, QUAL-03]

must_haves:
  truths:
    - "Dashboard shows an Accuracy section above the Signal Feed with aggregate stats (hit rate by tier, avg return by window)"
    - "Accuracy section auto-refreshes on each monitor cycle via HTMX SSE (same pattern as signal-rows)"
    - "Dashboard shows 'Insufficient data (X/20)' instead of a percentage when fewer than 20 resolved signals exist for a tier"
    - "Telegram /accuracy command returns hit rates by tier with the same minimum sample gate"
    - "GET /api/accuracy returns JSON accuracy stats"
    - "GET /api/accuracy/partial returns the HTMX partial (EJS rendered accuracy_stats.ejs)"
    - "Weak tier results are shown in the recent signal events table but excluded from primary hit-rate display"
  artifacts:
    - path: "src/api/routes/accuracy.ts"
      provides: "GET /api/accuracy REST endpoint and GET /api/accuracy/partial HTMX endpoint"
      exports: ["default (Fastify plugin)"]
    - path: "src/api/views/partials/accuracy_stats.ejs"
      provides: "HTMX partial: aggregate stats table + recent signal events table"
      contains: "signal_events"
    - path: "src/api/views/dashboard.ejs"
      provides: "Accuracy section before Signal Feed section"
      contains: "accuracy"
    - path: "src/api/server.ts"
      provides: "accuracy routes registered"
      contains: "accuracy"
    - path: "src/api/bot/commands.ts"
      provides: "/accuracy Telegram command"
      contains: "accuracy"
  key_links:
    - from: "src/api/views/dashboard.ejs"
      to: "/api/accuracy/partial"
      via: "hx-get on HTMX-refreshed tbody/section, hx-trigger=sse:cycle"
      pattern: "accuracy/partial"
    - from: "src/api/routes/accuracy.ts"
      to: "src/signals/accuracy.ts"
      via: "getAccuracyStats() call"
      pattern: "getAccuracyStats"
    - from: "src/api/bot/commands.ts"
      to: "src/signals/accuracy.ts"
      via: "getAccuracyStats() import"
      pattern: "getAccuracyStats"
---

<objective>
Wire the accuracy data to user-visible surfaces: a new dashboard section (HTMX-refreshing aggregate stats + recent signal events table) and a Telegram `/accuracy` command. Add the Fastify route plugin and register it in server.ts.

Purpose: QUAL-02 (accuracy rate visible to user) and QUAL-03 (historical outcome data surface enables manual weight calibration). This is the delivery layer — the data was collected in Plans 02 and 03; this plan makes it visible.
Output: Dashboard accuracy section, `/api/accuracy` REST + partial endpoints, Telegram `/accuracy` command.
</objective>

<execution_context>
@/Users/irfanmurad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/irfanmurad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/api/server.ts
@src/api/routes/signals.ts
@src/api/views/dashboard.ejs
@src/api/views/partials/signal_rows.ejs
@src/api/bot/commands.ts
@src/signals/accuracy.ts
@src/db/schema.ts
@.planning/phases/12-signal-accuracy-logging/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create accuracy route and accuracy_stats partial</name>
  <files>
    src/api/routes/accuracy.ts
    src/api/views/partials/accuracy_stats.ejs
    src/api/server.ts
  </files>
  <action>
**Create `src/api/routes/accuracy.ts`:**

Follow the same Fastify plugin pattern as `src/api/routes/signals.ts`. Two routes:

```typescript
import { FastifyInstance } from 'fastify';
import { getAccuracyStats, MIN_SAMPLE } from '../../signals/accuracy.js';
import { db } from '../../db/index.js';
import { signal_events } from '../../db/schema.js';
import { desc, eq } from 'drizzle-orm';

export default async function accuracyRoutes(app: FastifyInstance) {
  // REST: accuracy stats as JSON
  app.get('/api/accuracy', async (_req, reply) => {
    const stats = getAccuracyStats();
    return reply.send(stats);
  });

  // HTMX partial: renders accuracy_stats.ejs for dashboard refresh
  app.get('/api/accuracy/partial', async (_req, reply) => {
    const stats = getAccuracyStats();

    // Recent signal events: last 50, all tiers, ordered by fired_at DESC
    const recentEvents = db.select().from(signal_events)
      .orderBy(desc(signal_events.fired_at))
      .limit(50)
      .all();

    return reply.view('partials/accuracy_stats', { stats, recentEvents, MIN_SAMPLE });
  });
}
```

**Register in `src/api/server.ts`:**

Add `await app.register(import('./routes/accuracy.js'));` after the existing route registrations (after `status.js`). No other changes to server.ts.

**Create `src/api/views/partials/accuracy_stats.ejs`:**

Two sections: aggregate stats at top, recent signal events table below. Follow the project's EJS style (same as `signal_rows.ejs` — minimal HTML, no external CSS frameworks):

```html
<%
// Tier display order: strong, moderate, weak (in that order)
const TIER_ORDER = ['strong', 'moderate', 'weak'];
const statsMap = {};
(stats || []).forEach(function(s) { statsMap[s.tier] = s; });
%>

<!-- Aggregate accuracy stats by tier -->
<div class="accuracy-stats">
  <table>
    <thead>
      <tr>
        <th>Tier</th>
        <th>Signals</th>
        <th>Hit Rate (24h)</th>
        <th>Avg 1h</th>
        <th>Avg 4h</th>
        <th>Avg 24h</th>
      </tr>
    </thead>
    <tbody>
      <% TIER_ORDER.filter(function(t) { return t !== 'weak'; }).forEach(function(tier) { %>
      <%
        const s = statsMap[tier];
        const totalResolved = s ? s.total_resolved : 0;
        const hitRateStr = !s ? '—'
          : s.hit_rate_24h === null ? 'Insufficient data (' + totalResolved + '/20)'
          : (s.hit_rate_24h * 100).toFixed(1) + '%';
        const avg1h = s && s.avg_return_1h != null ? (s.avg_return_1h * 100).toFixed(1) + '%' : '—';
        const avg4h = s && s.avg_return_4h != null ? (s.avg_return_4h * 100).toFixed(1) + '%' : '—';
        const avg24h = s && s.avg_return_24h != null ? (s.avg_return_24h * 100).toFixed(1) + '%' : '—';
      %>
      <tr>
        <td><%= tier %></td>
        <td><%= totalResolved %></td>
        <td><%= hitRateStr %></td>
        <td><%= avg1h %></td>
        <td><%= avg4h %></td>
        <td><%= avg24h %></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
</div>

<!-- Recent signal events (all tiers including weak) -->
<div class="recent-events">
  <h3>Recent Signal Events</h3>
  <table>
    <thead>
      <tr>
        <th>Token</th>
        <th>Score</th>
        <th>Tier</th>
        <th>Entry Price</th>
        <th>1h</th>
        <th>4h</th>
        <th>24h</th>
        <th>Fired</th>
      </tr>
    </thead>
    <tbody>
      <% (recentEvents || []).forEach(function(e) { %>
      <%
        function fmtPct(pct, status) {
          if (status === 'failed') return 'rug';
          if (pct == null) return '—';
          return (pct >= 0 ? '+' : '') + (pct * 100).toFixed(1) + '%';
        }
        function fmtStatus(status) {
          if (!status) return '';
          if (status === 'hit') return ' ✓';
          if (status === 'miss') return ' ✗';
          return '';
        }
      %>
      <tr>
        <td><code><%= e.token_mint.slice(0, 8) %>...</code></td>
        <td><%= e.signal_score != null ? e.signal_score.toFixed(1) : '—' %></td>
        <td><%= e.tier %></td>
        <td><%= e.entry_price != null ? '$' + e.entry_price.toPrecision(4) : '—' %></td>
        <td><%= fmtPct(e.outcome_1h_pct, e.outcome_1h_status) %><%= fmtStatus(e.outcome_1h_status) %></td>
        <td><%= fmtPct(e.outcome_4h_pct, e.outcome_4h_status) %><%= fmtStatus(e.outcome_4h_status) %></td>
        <td><%= fmtPct(e.outcome_24h_pct, e.outcome_24h_status) %><%= fmtStatus(e.outcome_24h_status) %></td>
        <td><%= e.fired_at ? new Date(e.fired_at).toLocaleDateString() : '—' %></td>
      </tr>
      <% }); %>
      <% if (!recentEvents || recentEvents.length === 0) { %>
      <tr><td colspan="8" style="text-align:center;color:#888;">(no signal events yet)</td></tr>
      <% } %>
    </tbody>
  </table>
</div>
```
  </action>
  <verify>
`pnpm tsc --noEmit` — clean.
Start the server and verify: `curl http://localhost:3000/api/accuracy` returns JSON array (may be empty).
`curl http://localhost:3000/api/accuracy/partial` returns HTML (the EJS partial rendered).
  </verify>
  <done>accuracy routes registered and responding. /api/accuracy returns JSON stats. /api/accuracy/partial returns rendered EJS partial. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add accuracy section to dashboard and /accuracy Telegram command</name>
  <files>
    src/api/views/dashboard.ejs
    src/api/bot/commands.ts
  </files>
  <action>
**Modify `src/api/views/dashboard.ejs`:**

Add a new Accuracy section BEFORE the existing Signal Feed section. The section uses the same HTMX SSE pattern as the signal rows:

```html
<section>
  <h2>Signal Accuracy</h2>
  <div hx-ext="sse" sse-connect="/events/cycle">
    <div id="accuracy-stats"
      hx-get="/api/accuracy/partial"
      hx-trigger="sse:cycle"
      hx-target="#accuracy-stats"
      hx-swap="innerHTML">
      <%- include('partials/accuracy_stats', { stats: accuracyStats, recentEvents: recentSignalEvents, MIN_SAMPLE: 20 }) %>
    </div>
  </div>
</section>
```

This section goes immediately before the `<section>` block containing `<h2>Signal Feed</h2>`.

Also update the server-side render in `src/api/server.ts` to query and pass the initial data for SSR. In the `app.get('/')` handler (dashboard route), add queries for accuracy stats and recent signal events before the `reply.view()` call:

```typescript
const { getAccuracyStats, MIN_SAMPLE } = await import('../signals/accuracy.js');
const accuracyStats = getAccuracyStats();
const recentSignalEvents = db.select().from(signal_events)
  .orderBy(desc(signal_events.fired_at))
  .limit(50)
  .all();
```

Pass these variables to the view:
```typescript
return reply.view('dashboard', {
  rows,
  active,           // already exists
  probationary,     // already exists
  // ... other existing vars ...
  accuracyStats,
  recentSignalEvents,
  MIN_SAMPLE,
}, { layout: 'layout' });
```

Import `signal_events` in the dashboard route's dynamic imports block alongside the other schema tables.

**Add `import { MIN_SAMPLE }` note:** The `MIN_SAMPLE` constant must be exported from `src/signals/accuracy.ts` (established in Plan 02). Confirm the export exists in the accuracy.ts summary before referencing it here.

**Modify `src/api/bot/commands.ts`:**

Add the `/accuracy` command after the existing `/signal` command, before the `/start` command. Import `getAccuracyStats` and `MIN_SAMPLE`:

```typescript
import { getAccuracyStats, MIN_SAMPLE } from '../../signals/accuracy.js';
```

Command implementation (on-demand only — no automatic digest):

```typescript
// /accuracy — signal accuracy stats by tier
bot.command('accuracy', async (ctx) => {
  const stats = getAccuracyStats();

  if (stats.length === 0) {
    return ctx.reply('No resolved signal outcomes yet. Check back after 24h of monitoring.');
  }

  const statsMap = new Map(stats.map(s => [s.tier, s]));
  const lines = ['strong', 'moderate'].map(tier => {
    const s = statsMap.get(tier);
    if (!s) return `<b>${tier}:</b> No data`;
    if (s.total_resolved < MIN_SAMPLE) {
      return `<b>${tier}:</b> Insufficient data (${s.total_resolved}/${MIN_SAMPLE})`;
    }
    const hr = (s.hit_rate_24h! * 100).toFixed(1);
    const avg24 = s.avg_return_24h != null ? (s.avg_return_24h * 100).toFixed(1) + '%' : '—';
    const avg1h = s.avg_return_1h != null ? (s.avg_return_1h * 100).toFixed(1) + '%' : '—';
    return `<b>${tier}:</b> ${hr}% hit rate | 1h avg: ${avg1h} | 24h avg: ${avg24} | n=${s.total_resolved}`;
  });

  // Include weak tier stats if available (for tier differentiation info, not primary display)
  const weakStats = statsMap.get('weak');
  if (weakStats && weakStats.total_resolved >= MIN_SAMPLE) {
    const weakDir = weakStats.hit_rate_24h != null ? (weakStats.hit_rate_24h * 100).toFixed(1) + '% directional' : '—';
    lines.push(`<b>weak:</b> ${weakDir} | n=${weakStats.total_resolved}`);
  }

  await ctx.reply(
    `<b>Signal Accuracy</b>\n\nThresholds: Strong ≥+50%, Moderate ≥+25%, Weak=directional\n\n${lines.join('\n')}`,
    { parse_mode: 'HTML' }
  );
});
```

The `/accuracy` command is on-demand only (consistent with all other bot commands). No automatic digest scheduling.
  </action>
  <verify>
`pnpm tsc --noEmit` — clean.
Start the server: `pnpm run serve` (or equivalent). Open `http://localhost:3000` in a browser — Accuracy section appears above Signal Feed. The section shows "no signal events yet" tables initially.
`pnpm test` — full suite passes.
  </verify>
  <done>Dashboard accuracy section renders above Signal Feed and auto-refreshes on cycle events. Telegram /accuracy command registered and responds with tier stats. TypeScript clean. All tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` — zero TypeScript errors
- `pnpm test` — all tests pass (220+ suite unchanged by UI additions)
- `curl http://localhost:3000/api/accuracy` returns JSON (empty array if no events yet)
- `curl http://localhost:3000/api/accuracy/partial` returns rendered HTML
- Dashboard root `/` includes "Signal Accuracy" section before "Signal Feed" section
- Telegram `/accuracy` command is registered in commands.ts (confirmed by grep)
- Accuracy section uses HTMX `hx-trigger="sse:cycle"` pattern consistent with signal-rows
</verification>

<success_criteria>
The full Phase 12 delivery is complete: signal events are logged on tier transitions, outcomes are resolved automatically on each cycle, and accuracy stats are visible on both the dashboard and via Telegram `/accuracy`. All three requirement IDs are addressed: QUAL-01 (event logging in Plans 01+03), QUAL-02 (accuracy rate surface in Plans 02+04), QUAL-03 (full snapshot data for calibration in Plans 01+03, surfaced in Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/12-signal-accuracy-logging/12-04-SUMMARY.md` summarising:
- Files modified and what changed
- Dashboard section placement and HTMX pattern used
- Telegram command behavior (on-demand, strong/moderate primary, weak secondary)
- Total test count confirmed green
- Phase 12 complete marker
</output>
